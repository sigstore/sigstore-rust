# tuf-conformance expected failures: one test name per line (a bare `test_...`
# name covers every parametrization). Comment lines are ignored. xfail is
# strict, so a listed test that passes fails the run. Currently none.
